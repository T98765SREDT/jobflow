# Screenshots

The seeded dashboard is ready for a portfolio screenshot. Run JobFlow with a fresh database, set the browser viewport near 1440 × 900, and save the unfiltered dashboard as `jobflow-dashboard.png` in this directory.

Before publishing the repository, verify that the screenshot contains only the fictional demo companies shipped in `jobflow/database.py` and no personal application data.
